# ld65 链接脚本 - NROM-128 (Mapper 0)
# 输出布局: 16 字节 iNES 头 + 16KB PRG-ROM + 8KB CHR-ROM

MEMORY {
    ZP:     start = $0000, size = $0100, type = rw, file = "";
    HEADER: start = $0000, size = $0010, type = ro, file = %O, fill = yes, fillval = $00;
    PRG:    start = $C000, size = $4000, type = ro, file = %O, fill = yes, fillval = $FF;
    CHR:    start = $0000, size = $2000, type = ro, file = %O, fill = yes, fillval = $00;
}

SEGMENTS {
    ZEROPAGE: load = ZP,     type = zp;
    HEADER:   load = HEADER, type = ro;
    CODE:     load = PRG,    type = ro, start = $C000;
    VECTORS:  load = PRG,    type = ro, start = $FFFA;
    CHARS:    load = CHR,    type = ro;
}
